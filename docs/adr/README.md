# Architecture Decision Records — index

One file per architectural decision (Context → Decision → Rationale → Alternatives → Consequences). **Do not silently break an ADR** — update it instead. All are **Accepted** unless noted.

| # | Title | One-line summary |
|---|---|---|
| [0001](0001-primary-user-student-and-mvp-scope.md) | Primary user = student; initial scope | The agent is a "think + answer" partner for students; wedge = deep school dossier; chancing/personalization/memory/writing deferred. |
| [0002](0002-tracked-schools-only-scope.md) | Any school in scope | Boundary = profiled in the database or not; old tracked/tier details replaced by ADR 0032's selected-edition coverage. |
| [0003](0003-runtime-pydanticai-langgraph.md) | Runtime: PydanticAI + LangGraph | Model-agnostic, MCP-native, typed outputs + multi-agent/HITL/state; resolves the Claude-Agent-SDK model-lock-in tension. |
| [0004](0004-db-access-as-mcp-server.md) | DB access as an MCP server | The data-access layer is the `counselle-db` MCP server (Python, asyncpg, read-only); tool logic lives in an in-process service layer the MCP server thinly wraps — `app/` imports the service directly, only the LLM's tool loop goes through MCP. |
| [0005](0005-three-layer-data-access-and-sql-escape-hatch.md) | Data access + SQL escape hatch | Service/MCP + guarded-SQL decision retained; old tool layers replaced by ADR 0032's four tools. |
| [0006](0006-reading-rules-and-citations-in-code.md) | Reading rules + citations in code | Code-owned honesty retained; old flat-field details replaced by ADR 0032's packet/evidence rules. |
| [0007](0007-hybrid-field-discovery.md) | Hybrid field discovery | **Superseded by ADR 0032.** |
| [0008](0008-field-embedding-reconciliation.md) | Field-embedding reconciliation | **Superseded by ADR 0032.** |
| [0009](0009-deep-research-gpt-researcher.md) | Deep research: GPT-Researcher | Embedded, cost-optimized (cheap-model routing, capped depth, DB-first); won the 4-way bake-off. Graph topology `prepare → agent → END`; the research node attaches additively. |
| [0010](0010-skills-via-skill-md.md) | Skills via SKILL.md | Skills authored in the open SKILL.md standard; skills = workflow, MCP = transport. |
| [0011](0011-model-config-env-litellm.md) | Model config via env + optional LiteLLM | Per-agent `model=` from env; optional LiteLLM sidecar for fallbacks/budgets/open models. |
| [0012](0012-read-only-db-role.md) | Read-only DB role | Read-only boundary retained; ADR 0032 narrows it to the five `cds_library_reader` views. |
| [0013](0013-source-control-dropdown.md) | Per-request source control | Web/Reddit(per-community)/.edu toggles gate the toolset; enforced in orchestration. |
| [0014](0014-visualization-render-spec.md) | Visualizations: render-spec & provenance boundary | Viz in scope (stat block, comparison table; score band removed — ADR 0024); LLM picks the shape, the tool fetches the numbers (never via the LLM); one `render_viz` tool → JSON spec → dumb frontend component. |
| [0015](0015-external-search-tavily.md) | External search via Tavily | All 3 external searches = Tavily scoped by domain (3 thin tools); Reddit agent-steered from a labeled menu; deep research stays GPT-Researcher (Tavily underneath), not a hosted research black box. |
| [0016](0016-api-first-event-protocol.md) | API-first service + event protocol | The agent service exposes a versioned SSE event protocol (`meta`/`delta`/`viz`/`clarify`/`sources`/`usage`/`done`/`error`); every frontend is a client; auth is additive (ADR 0021). |
| [0017](0017-layered-core-stack-native-seams.md) | Layered core, stack-native seams | Four layers (domain → app → adapters → api), dependencies inward only; the pure domain core holds the honesty logic; use PydanticAI/MCP/LangGraph's own seams, never wrap them; no pass-through modules. |
| [0018](0018-central-config-and-data-assets.md) | Central config + data assets | One fail-fast Settings surface + versioned editorial assets + live-DB-derived facts; ADR 0032 removes the old shortlist/catalog assets. |
| [0019](0019-platform-ready-sessions.md) | Platform-ready sessions | Durable sessions via LangGraph's Postgres checkpointer in `counselle.*` from day one; `session_id` required, `user_id` nullable (auth is additive — ADR 0021); Counselle owns its schema + migrations. |
| [0020](0020-frontend-librechat-clone.md) | Frontend: LibreChat clone | Historical MVP2 decision. Superseded for active frontend work by ADR 0026. |
| [0021](0021-auth-fastapi-users-cookie-jwt.md) | Auth: fastapi-users, cookie JWT, Google OAuth | fastapi-users under `/v1/auth/*` with a JWT in an httpOnly same-origin cookie (the SSE-forced choice; TTL 30 days, no refresh); custom asyncpg user store; `owned_session` = the one ownership dependency (foreign/unknown → 404). |
| [0022](0022-protocol-work-visibility-resume-cancel.md) | Protocol: step/thinking, resume, cancel | Additive-within-v1 `step`/`thinking` events + the turn registry (detached turns, Last-Event-ID resume, cancel, single-writer); message identity, the turn record, history-rewrite edit/regenerate, the clarify-park lifecycle, and cancel/watchdog semantics. |
| [0023](0023-spa-same-origin-one-deployable.md) | One deployable: SPA served same-origin | The FastAPI service serves the built SPA + landing page from one container (multi-stage build); kills CORS, makes cookie auth trivial; statelessness amended to two named in-process owners (turn registry, rate counters); entrypoint runs migrations. |
| [0024](0024-remove-score-band.md) | Remove the score_band viz | The SAT/ACT score band is dropped (it added nothing over a stat block/prose); `RenderSpec.type` narrows to `stat_block \| comparison_table`; the SAT-composite honesty rule moves from the tool validator to prompt/skill guidance. Supersedes ADR 0014 on that point. |
| [0025](0025-turn-persistence-module.md) | Turn persistence module | Terminal turn persistence, lifecycle predicates, and the empty-partial prose rule live in `app/turn_persistence.py`, so every terminal path writes the same transcript shape. |
| [0026](0026-mvp3-frontend-reset.md) | MVP3 frontend reset | The active frontend is rebuilt from the MVP3 design system and workspace shell; no LibreChat vendor/tokens/naming, while the old backup remains a reference for the backend client contract. |
| [0027](0027-workspace-service-and-change-events.md) | Workspace service + change events | MVP3 workspace mutations share one `app/workspace/` service layer, persist actor-attributed change rows, and publish thin post-commit events through a bounded runtime event bus. |
| [0028](0028-the-run-is-the-message.md) | The run is the message | The assistant response is the whole run; `agent.iter(...)` is the runtime seam; active-run coordination stays in a process-local `RunHandleStore`; stop suspends with partial snapshots; mid-run steering is `POST /steer` + `user_message`; `thinking_stream` is the live default. |
| [0029](0029-agent-workspace-tools-direct-service.md) | Agent workspace tools call the service layer directly | Six task tools call `service_tasks.py` in-process (not MCP/HTTP), mount-gated on authenticated `user_id` via `turn_ids` (unmounted, not hidden); new `workspace` `StepKind`; errors are teaching payloads, not exceptions; safety model is reversibility (soft archive + restore) over code-level confirmation gates. |
| [0030](0030-essay-markdown-projection.md) | Essay content as a markdown projection, direct edits | The agent reads/edits essays as markdown (`essay_markdown.py`), never Tiptap JSON; `edit_essay` does exact-string edits like Claude Code's Edit tool, block-preserving on reparse; `expected_version` guards every content write against a stale read (`FOR UPDATE` closes a TOCTOU race in the guard itself); direct writes now, shaped so a future suggestions layer can wrap the same edit mechanic. |
| [0031](0031-student-profile-documents-and-memory.md) | Student profile, documents, and agent memory: three stores, not one | Typed `Profile`, `Document` list, and curated `Memory` pile are three separate stores riding the ADR 0027/0029 workspace pattern; memory is written only through explicit `remember`/`update_memory`/`forget` tool calls (never automatic or vector-retrieved), rendered whole-in-context per turn (`student_context.py`) alongside a hard 5,000-char budget; documents are bytea-in-Postgres with server-side extraction, honesty locked by `text_status` and verbatim scalar rendering. |
| [0032](0032-db-rewire-cds-library.md) | Rewire database access to the CDS Library | Five reader views, four tools, dynamic manifest 5.0.1, packet-v8 anti-corruption boundary, evidence markers, and open viz-v2 seam replace the retired wide field store. |
| [0033](0033-reserved-settings-namespace-and-locked-merge.md) | Reserved settings namespace + locked merge | `/v1/me` treats reserved server-owned settings keys as immutable from clients, and settings updates merge against a locked fresh row so concurrent preference writes do not clobber each other. |
| [0034](0034-counselor-response-modes.md) | Counselor response modes | Quick/Think are server-owned counselor modes: Quick maps to Gemini 3.5 Flash, Think maps to Gemini 3.1 Pro Preview with gated provider thoughts; browsers choose only the mode id, never model/thinking parameters. |

> Some docs reference **pipeline** ADRs (in `~/Projects/counselle-data-pipeline/docs/adr/`, the independently deployed CDS Library repo) — e.g. pipeline ADR 0001 (parameterized SQL only) and pipeline ADR 0004 (CDS never auto-collects). Those are separate from this repo's ADRs above.
