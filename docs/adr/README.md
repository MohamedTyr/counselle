# Architecture Decision Records — index

One file per architectural decision (Context → Decision → Rationale → Alternatives → Consequences). **Do not silently break an ADR** — update it instead. All are **Accepted** unless noted.

| # | Title | One-line summary |
|---|---|---|
| [0001](0001-primary-user-student-and-mvp-scope.md) | Primary user = student; MVP1 scope | MVP1 is a "think + answer" agent for students; wedge = deep school dossier; chancing/personalization/memory/writing deferred. |
| [0002](0002-tracked-schools-only-scope.md) | Any school in scope; "tracked" = CDS tier | The agent works on any school in the DB (2,746); `is_tracked` is repurposed as a CDS-coverage signal (extracted / PDF-only / base), not a gate. Boundary = in-the-DB or not. *(Revised — supersedes the original tracked-only scope.)* |
| [0003](0003-runtime-pydanticai-langgraph.md) | Runtime: PydanticAI + LangGraph | Model-agnostic, MCP-native, typed outputs + multi-agent/HITL/state; resolves the Claude-Agent-SDK model-lock-in tension. |
| [0004](0004-db-access-as-mcp-server.md) | DB access as an MCP server | The data-access layer is the `counselle-db` MCP server (Python, asyncpg, read-only); tool logic lives in an in-process service layer the MCP server thinly wraps — `app/` imports the service directly, only the LLM's tool loop goes through MCP. |
| [0005](0005-three-layer-data-access-and-sql-escape-hatch.md) | Three-layer data access + SQL escape hatch | Field discovery → safe typed tools → guarded read-only SQL escape hatch (included in MVP1). |
| [0006](0006-reading-rules-and-citations-in-code.md) | Reading rules + citations in code | The value-reading rules (R1–R12) and provenance live in code; every value is a citation envelope. |
| [0007](0007-hybrid-field-discovery.md) | Hybrid field discovery | Static category map (common path) + pgvector semantic search (long tail) over the 1,093-field catalog + always-on keyword/trigram fallback (new fields never invisible). |
| [0008](0008-field-embedding-reconciliation.md) | Field-embedding reconciliation | The vector index is a self-healing derived cache of `fields` (hash-diff reconcile + keyword fallback so new fields are never invisible). |
| [0009](0009-deep-research-gpt-researcher.md) | Deep research: GPT-Researcher | Embedded, cost-optimized (cheap-model routing, capped depth, DB-first); won the 4-way bake-off. *(Subsystem deferred from MVP1 — stub seam in the graph; activates in the follow-up plan.)* |
| [0010](0010-skills-via-skill-md.md) | Skills via SKILL.md | Skills authored in the open SKILL.md standard; skills = workflow, MCP = transport. |
| [0011](0011-model-config-env-litellm.md) | Model config via env + optional LiteLLM | Per-agent `model=` from env; optional LiteLLM sidecar for fallbacks/budgets/open models. |
| [0012](0012-read-only-db-role.md) | Read-only DB role | A dedicated `counselle_ro` role (SELECT only, read-only txn, statement timeout, row cap). |
| [0013](0013-source-control-dropdown.md) | Per-request source control | Web/Reddit(per-community)/.edu toggles gate the toolset; enforced in orchestration. |
| [0014](0014-visualization-render-spec.md) | Visualizations: render-spec & provenance boundary | MVP1 = 3 viz (stat block, comparison table, score band); LLM picks the shape, the tool fetches the numbers (never via the LLM); one `render_viz` tool → JSON spec → dumb frontend component. |
| [0015](0015-external-search-tavily.md) | External search via Tavily | All 3 external searches = Tavily scoped by domain (3 thin tools); Reddit agent-steered from a labeled menu; deep research stays GPT-Researcher (Tavily underneath), not a hosted research black box. |
| [0016](0016-api-first-event-protocol.md) | API-first service + event protocol | The agent service exposes a versioned SSE event protocol (`meta`/`delta`/`viz`/`clarify`/`sources`/`usage`/`done`/`error`); every frontend is a client; auth landed in MVP2 (ADR 0021). |
| [0017](0017-layered-core-stack-native-seams.md) | Layered core, stack-native seams | Four layers (domain → app → adapters → api), dependencies inward only; the pure domain core holds the honesty logic; use PydanticAI/MCP/LangGraph's own seams, never wrap them; no pass-through modules. |
| [0018](0018-central-config-and-data-assets.md) | Central config + data assets | One fail-fast typed Settings surface (pydantic-settings) + versioned data assets (prompts, subreddit menu, dossier shortlist, season table) + live-DB-derived facts; hardcoding reserved for true invariants. |
| [0019](0019-platform-ready-sessions.md) | Platform-ready sessions | Durable sessions via LangGraph's Postgres checkpointer in `counselle.*` from day one; `session_id` required, `user_id` nullable until the platform phase; Counselle owns its schema + migrations. |
| [0020](0020-frontend-librechat-clone.md) | MVP2 frontend: LibreChat clone | The SPA's design system + chat-commodity components are cloned from LibreChat (MIT) on its own stack (React/Vite/Tailwind/Radix/Jotai); tokens copied wholesale, components vendored in quarantine; the Counselle honesty surfaces (timeline, cards, citations, clarify) built new in the same visual language. |
| [0021](0021-auth-fastapi-users-cookie-jwt.md) | Auth: fastapi-users, cookie JWT, Google OAuth | fastapi-users under `/v1/auth/*` with a JWT in an httpOnly same-origin cookie (the SSE-forced choice; TTL 30 days, no refresh); custom asyncpg user store; `owned_session` = the one ownership dependency (foreign/unknown → 404). |
| [0022](0022-protocol-work-visibility-resume-cancel.md) | Protocol: step/thinking, resume, cancel | Additive-within-v1 `step`/`thinking` events + the turn registry (detached turns, Last-Event-ID resume, cancel, single-writer); amended at B0 with message identity, the turn record, history-rewrite edit/regenerate, the clarify-park lifecycle, and cancel/watchdog semantics (G1–G5). |
| [0023](0023-spa-same-origin-one-deployable.md) | One deployable: SPA served same-origin | The FastAPI service serves the built SPA + landing page from one container (multi-stage build); kills CORS, makes cookie auth trivial; statelessness amended to two named in-process owners (turn registry, rate counters); entrypoint runs migrations. |

> Some docs reference **pipeline** ADRs (in `~/Projects/ascensia-data-pipeline/docs/adr/`) — e.g. pipeline ADR 0001 (parameterized SQL only) and pipeline ADR 0004 (CDS never auto-collects). Those are separate from this repo's ADRs above.
