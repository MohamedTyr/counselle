/**
 * Static footer brand copy (B5c). `/v1/config` does NOT serve the footer — it is
 * fixed product copy, not season-keyed config — so it lives here as a constant
 * the vendored `Chat/Footer.tsx` reads directly.
 */
export const APP_FOOTER =
  'Counselle can make mistakes — every number is cited; check the source. | Data: CDS · IPEDS · College Scorecard';
