// Default-export alias for upstream files that import `cn from '~/utils/cn'`
// (CodeBlock, CopyButton). Same helper as the named export in '~/utils'.
import { cn } from '@librechat/client/utils/utils';

export default cn;
